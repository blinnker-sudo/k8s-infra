apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nest-api-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/blinnker-sudo/k8s-infra.git
    targetRevision: HEAD
    path: helm/nest-api
    helm:
      valueFiles:
        - values.yaml
        - values-dev.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
